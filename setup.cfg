[pycodestyle]
max-line-length = 100

[flake8]
max-line-length = 100
# E203 disagrees with black on slice spacing; W503 disagrees on line breaks
# before binary operators.
extend-ignore = E203, W503
exclude = .git,__pycache__,build,dist

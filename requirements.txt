numpy>=1.21
# Optional. Used only as an alternative to our own Hungarian implementation
# (swarmplan.assignment.solve_assignment) and as the cross-check the test suite
# measures it against. Everything runs without it.
scipy>=1.7
# Optional. Needed only for the figures and animations under benchmarks/.
matplotlib>=3.5
# Optional. Needed only to write the animated GIFs.
pillow>=9.0
